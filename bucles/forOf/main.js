const cars = ["BMW", "Volvo", "Mini"];

for (let x of cars) {
  console.log(x)        // devulve los valores de la lista, y no indices como 'in'
};

let lenguajes = 'HolaBuendia';

for (let x of lenguajes) {
  console.log(x);           // devuelve cada caracter de lenguajes
};